<manifest xmlns:android="http://schemas.android.com/apk/res/android"><!-- required="true"：如果设备没有摄像头，应用就无法安装；required="false"：即使设备没有摄像头，应用也能安装并运行，不过可能会提示用户无法使用某些功能 -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- 允许使用闪光灯功能 -->
    <!-- <uses-permission android:name="android.permission.FLASHLIGHT"/> -->
    <!-- 允许访问摄像头 -->
    <!-- <uses-permission android:name="android.permission.CAMERA"/> -->
    <!-- 允许写入外部存储，用于保存或修改相册中的图片 -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> -->
    <!-- Android 10（API 29）及以上，访问媒体文件的位置信息权限 -->
    <!-- <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" /> -->
    <!-- 读取外部存储，用于访问设备上的文件 -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> -->
    <!-- 写入外部存储，用于修改或保存设备上的文件 -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->
    <!-- 允许应用访问互联网，用于网络通信、文件下载、视频流等 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许应用访问设备的网络连接状态（如是否联网） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许应用访问Wi-Fi状态信息（如Wi-Fi是否开启、连接的SSID等） -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 列出需要检查的 URL schemes -->
    <queries>
        <!-- 如果应用需要检查 SMS 链接 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="sms" />
        </intent>
        <!-- 如果应用需要检查电话链接 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <!-- 如果应用需要检查 HTTP 或 HTTPS 链接 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- 如果应用需要检查 InAppBrowser -->
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>
    <application
        android:enableOnBackInvokedCallback="true"
        android:label="水哥自用框架"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- image_cropper 图片裁剪 activity 申明 -->
        <activity android:name="com.yalantis.ucrop.UCropActivity"
          android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <!-- 指定默认主题，启动时显示 -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
            />
            <!-- 配置主启动 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 配置 URL Scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="dengzemiao"/>
                <!-- <data android:scheme="dengzemiao" android:host="pay"/> -->
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
